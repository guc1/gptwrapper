version: "3.9"
services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: chatbot
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: chatbot
    ports: ["5432:5432"]
